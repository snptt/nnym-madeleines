{
  "buildCommand": "git submodule update --init --recursive && hugo version && hugo --gc --minify",
  "outputDirectory": "public",
  "installCommand": "echo '确保使用最新Hugo版本'",
  "functions": {},
  "framework": "hugo",
  "builds": [
    {
      "src": "**/*",
      "use": "@vercel/static-build"
    }
  ]
}
